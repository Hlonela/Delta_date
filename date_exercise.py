from datetime import datetime, timedelta, time

#for days in range ():
#dt = datetime (2021, 5, 18)
#answer = dt+timedelta(days=7)
#x=datetime.now()
#print(answer)
#print(x)

init_time_for_now = datetime.now()
#Printing initial date
print("Initial date:", str(init_time_for_now))

#Calculating future dates

#Using a for loop


dt = datetime (2021, 5, 18)
year = dt.year
print(year)

age = year - 1997
print("I am"+" "+ str(age) +" "+ "years old")


#x = 0


#while x < 10:
    #x = x+1
   # init_time_for_now = init_time_for_now + timedelta (days=7)
    #print(init_time_for_now)


